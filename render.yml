services:
  - type: web
    name: my-bun-app
    runtime: docker # <-- Change from 'node' to 'docker'
    # Remove 'buildCommand' and 'startCommand' lines if they are present, 
    # as Docker handle these inside the image.
